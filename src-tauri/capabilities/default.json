{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for the BakkesSwap shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
